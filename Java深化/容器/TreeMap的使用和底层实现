1.TreeMap的使用

Key:唯一，有序，是升序的(可以通过外部比较器改变排序的规则，例如按英文字母的个数进行排序)

底层数据结构:红黑树

2.TreeMap的底层实现

如果使用 TreeMap 存储自定义对象做为 key 时，要求必须具备比较规则，否则运行报错

#### 3.内部比较器要实现comparable接口，外部比较器要实现comparator接口

4.如果调用TreeMap的无参构造方法，则默认调用内部比较器进行比较排序，如果调用TreeMap的带参构造方法，则调用给定的外部比较器进行比较排序（或者说传递一个comparator对象）（即通过创建一个自定义类，实现了comparator接口的compare方法，compare方法用于给定比较排序规则）



