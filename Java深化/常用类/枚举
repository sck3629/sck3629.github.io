**1** **枚举的定义** 

只能够取特定值中的一个 使用 enum 关键字 

**2** **枚举的使用** 

每个被枚举的成员实质就是一个枚举类型的实例，他们 默认都是 public static final 的。可以直接通过枚举类型名直接 

使用它们。强烈建议当你需要定义一组常量时，使用枚举类型尽量不要使用枚举的高级特性，事实上高级特性都可以使用 

普通类来实现，没有必要引入复杂性！ 

**3** **枚举与类的关系** 

所有的枚举类型隐性地继承自 java.lang.Enum。(枚举实质上还是类！)