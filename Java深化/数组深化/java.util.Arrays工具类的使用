1.Arrays类中的常用方法

1) toString()打印数组 (与Object类中的toString()方法不同)

2) equals()比较两个数组是否相同 (与Object类中equals()方法不同)

3) copyOf(…)复制指定的数组 (效率低,每次复制都要开辟新的空间,无论目标数组的大小是否够用) 

4) fill(…)填充  

5) sort(…)数组排序 升序排序(排序的对象具备比较大小的能力) 对象数组的排序 (需要编写比较的规则)